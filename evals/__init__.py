"""Evaluation harness: MLflow-logged suite runs, judge calibration, the gate.

Three entry points, all runnable from the repository root:

    python -m evals.run_mlflow      run the suite and log one MLflow run
    python -m evals.calibrate_judge compute Cohen's kappa judge vs. hand labels
    python -m evals.gate            fail the build below any acceptance threshold

The cases themselves live in evals/cases/ as data (input document + hand
authored case.json) and are generated by scripts/generate_data.py. Nothing in
this package changes how the pipeline behaves; it only measures it.
"""
